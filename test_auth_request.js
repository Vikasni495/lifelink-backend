const https = require('node:https'); const http = require('node:http'); const data1 = JSON.stringify({fullName:'Admin User', email:'admin@lifelink.local', password:'test123', phone:'1234567890', bloodGroup:'O+', district:'Test'}); const options1 = {hostname:'localhost', port:8082, path:'/api/auth/register', method:'POST', headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(data1)}}; const req1 = http.request(options1, res=>{let body=''; res.on('data', chunk=>body+=chunk); res.on('end', ()=>{console.log('REGISTER', res.statusCode, body)});}); req1.on('error', e=>console.error('req1 error', e)); req1.write(data1); req1.end(); const data2 = JSON.stringify({email:'admin@lifelink.local', password:'test123'}); const options2 = {hostname:'localhost', port:8082, path:'/api/auth/login', method:'POST', headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(data2)}}; const req2 = http.request(options2, res=>{let body=''; res.on('data', chunk=>body+=chunk); res.on('end', ()=>{console.log('LOGIN', res.statusCode, body)});}); req2.on('error', e=>console.error('req2 error', e)); req2.write(data2); req2.end();
